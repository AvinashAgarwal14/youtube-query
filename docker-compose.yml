version: '3.7'
services:
  api:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    container_name: youtube-query-api
    links:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27018:27017"
    container_name: mongo